// Minimal CodeMirror mode stub for tests (bar)
(function () {
  if (typeof window === 'undefined') return
  window.CodeMirror = window.CodeMirror || {}
  window.CodeMirror.modes = window.CodeMirror.modes || {}
  window.CodeMirror.modes.bar = { name: 'bar' }
})()
// placeholder mode stub for tests
/* empty mode stub */
